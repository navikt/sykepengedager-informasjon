kafka.brokers: localhost://9092
kafka.truststore.path: /truststore
kafka.keystore.path: /keystore
kafka.credstore.password: /credstore/password
kafka.schema.registry.user: "kafka.schema.registry.user"
kafka.schema.registry.password: "kafka.schema.registry.password"
kafka.schema.registry: "kafka.schema.registry"
spring.flyway.clean-disabled: "false"
azure:
  app:
    client:
      id: "client-id"
      secret: "client-secret"
  openid:
    config:
      token:
        endpoint: "https://token-endpoint"


istilgangskontroll.url": "http://istilgangskontrollUrl:9099"
istilgangskontroll.scope: "istilgangskontrollScope"
ditt.sykefravaer.client.id: "dittSykefravaerClientId"
meroppfolging.backend.client.id: "meroppfolgingBackendClientId"
meroppfolging.frontend.client.id: "meroppfolgingFrontendClientId"

